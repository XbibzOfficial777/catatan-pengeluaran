import 'package:flutter/material.dart';

/// Palet warna aplikasi (dipusatkan; sebelumnya konstanta privat main.dart).
///
/// Aksen utama Cursor Orange dipakai hemat; warna semantik untuk error dan
/// sukses dipakai konsisten di seluruh UI.
const cursorOrange = Color(0xFFF54E00);
const cursorOrangeDark = Color(0xFFD04200);
const semanticError = Color(0xFFCF2D56);
const semanticMint = Color(0xFF1F8A65);
const warmInk = Color(0xFF26251E);
const warmSlate = Color(0xFF5A5852);
const canvasLight = Color(0xFFF7F7F4); // warm cream canvas
const canvasDark = Color(0xFF26251E);
const surfaceDark = Color(0xFF333129);


