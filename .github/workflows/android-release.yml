name: Android Release
runs-on: self-hosted
on:
  push:
    tags:
      - 'v*.*.*'
  workflow_dispatch:
    inputs:
      release_tag:
        description: 'Tag release tujuan, misalnya v1.3.8'
        required: true
        default: 'v1.3.8'
        type: string

permissions:
  contents: write

concurrency:
  group: android-release-${{ github.ref }}
  cancel-in-progress: true

jobs:
  release:
    name: Fast test and build universal plus arm64-v8a APKs
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Set up Java 21
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '21'
          cache: gradle

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          flutter-version: '3.47.0'
          cache: true

      - name: Cache pub packages
        uses: actions/cache@v6
        with:
          path: ~/.pub-cache
          key: ${{ runner.os }}-pub-${{ hashFiles('pubspec.lock') }}
          restore-keys: |
            ${{ runner.os }}-pub-

      - name: Configure release signing
        env:
          KEYSTORE_BASE64: ${{ secrets.ANDROID_KEYSTORE_BASE64 }}
          STORE_PASSWORD: ${{ secrets.ANDROID_KEYSTORE_PASSWORD }}
          KEY_PASSWORD: ${{ secrets.ANDROID_KEY_PASSWORD }}
        run: |
          test -n "$KEYSTORE_BASE64" || (echo 'Missing ANDROID_KEYSTORE_BASE64 secret' >&2; exit 1)
          test -n "$STORE_PASSWORD" || (echo 'Missing ANDROID_KEYSTORE_PASSWORD secret' >&2; exit 1)
          test -n "$KEY_PASSWORD" || (echo 'Missing ANDROID_KEY_PASSWORD secret' >&2; exit 1)
          mkdir -p android/keystores
          printf '%s' "$KEYSTORE_BASE64" | base64 --decode > android/keystores/xbibzofcv1.jks
          cat > android/key.properties <<EOF
          storePassword=$STORE_PASSWORD
          keyPassword=$KEY_PASSWORD
          keyAlias=xbibzofcv1
          storeFile=keystores/xbibzofcv1.jks
          EOF
          chmod 600 android/key.properties android/keystores/xbibzofcv1.jks

      - name: Install dependencies
        run: flutter pub get

      - name: Analyze and test
        run: |
          flutter analyze --no-fatal-infos --no-fatal-warnings
          flutter test

      - name: Inspect GitHub Actions runner
        run: |
          echo '--- Runner identity ---'
          uname -a
          echo '--- CPU / chip ---'
          lscpu
          echo '--- CPU model ---'
          grep -m1 -E 'model name|Hardware|Processor' /proc/cpuinfo || true
          echo '--- Memory ---'
          free -h
          grep -E 'MemTotal|MemAvailable|SwapTotal' /proc/meminfo
          echo '--- Disk ---'
          df -h /

      - name: Configure fast CI build
        run: |
          sed -i -E 's/-Xmx[0-9]+m/-Xmx4g/' android/gradle.properties
          sed -i 's/org.gradle.workers.max=1/org.gradle.workers.max=2/' android/gradle.properties
          sed -i 's/org.gradle.parallel=false/org.gradle.parallel=true/' android/gradle.properties
          # build.gradle.kts reads this property and disables R8/resource shrinking only in CI.
          echo 'ORG_GRADLE_PROJECT_fastRelease=true' >> "$GITHUB_ENV"

      # The Android toolchain is intentionally pinned to AGP 8.11.1/KGP 2.1.21
      # for Flutter 3.32/FlutLab compatibility. Flutter 3.47's validator asks
      # for newer versions even though this Kotlin DSL build remains valid.
      - name: Build universal fast release APK
        run: >-
          flutter build apk --release
          --no-shrink --no-tree-shake-icons
          --android-skip-build-dependency-validation

      - name: Preserve universal APK before split build
        run: |
          mkdir -p release-assets
          cp build/app/outputs/flutter-apk/app-release.apk release-assets/Catatan-Pengeluaran-universal.apk
          cp build/app/outputs/apk/release/app-release.apk.idsig release-assets/Catatan-Pengeluaran-universal.apk.idsig

      - name: Build arm64-v8a split fast release APK
        run: >-
          flutter build apk --release --split-per-abi
          --target-platform android-arm64
          --no-shrink --no-tree-shake-icons
          --android-skip-build-dependency-validation

      - name: Prepare release assets
        run: |
          mkdir -p release-assets
          cp build/app/outputs/apk/release/app-arm64-v8a-release.apk release-assets/Catatan-Pengeluaran-arm64-v8a.apk
          cp build/app/outputs/apk/release/app-arm64-v8a-release.apk.idsig release-assets/Catatan-Pengeluaran-arm64-v8a.apk.idsig
          sha256sum release-assets/Catatan-Pengeluaran-universal.apk release-assets/Catatan-Pengeluaran-universal.apk.idsig release-assets/Catatan-Pengeluaran-arm64-v8a.apk release-assets/Catatan-Pengeluaran-arm64-v8a.apk.idsig > release-assets/SHA256SUMS.txt

      - name: Upload workflow artifact
        uses: actions/upload-artifact@v7
        with:
          name: catatan-pengeluaran-apks-${{ github.ref_name }}
          path: release-assets/*
          if-no-files-found: error
          retention-days: 30

      - name: Publish GitHub Release
        if: startsWith(github.ref, 'refs/tags/') || github.event_name == 'workflow_dispatch'
        uses: softprops/action-gh-release@v3
        with:
          tag_name: ${{ github.event_name == 'workflow_dispatch' && inputs.release_tag || github.ref_name }}
          target_commitish: ${{ github.sha }}
          name: Catatan Pengeluaran ${{ github.event_name == 'workflow_dispatch' && inputs.release_tag || github.ref_name }}
          generate_release_notes: true
          files: release-assets/*
          body: |
            APK optimized Catatan Pengeluaran untuk Android 7–16.

            - Output universal: `Catatan-Pengeluaran-universal.apk`.
            - Output arm64-v8a: `Catatan-Pengeluaran-arm64-v8a.apk`.
            - CI memakai fast mode untuk universal dan arm64-v8a agar release cepat; APK sandbox tetap dibuild dengan R8 minification dan resource shrinking penuh.
            - Release ditandatangani dengan APK Signature Scheme v2, v3, dan v4; v1/JAR signing dinonaktifkan.
            - Fast mode CI menonaktifkan shrink/obfuscation hanya untuk kecepatan; APK sandbox dapat dibuild dengan kompresi penuh.
            - `Catatan-Pengeluaran-arm64-v8a.apk.idsig` adalah berkas signature v4 untuk Android 11+.
            - `SHA256SUMS.txt` tersedia untuk verifikasi checksum APK dan idsig.
